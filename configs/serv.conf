
  {
    listen         8080;
    server_name    localhost;
    max_body_size  100;

    location / {
        allowed_methods GET;

        root  html;
        index index.html;
    }

    location /put_test/ {
        allowed_methods PUT;
        root html;
    }
    location /post_body/ {
        allowed_methods POST;

        cgi_extension  .bla;
        cgi_path       cgi_tester;
    }
     location /directory/ {
        allowed_methods GET;
        root   html/YoupiBanane;
        index  youpi.bad_extension;

        cgi_extension  .bla;
        cgi_path       cgi_tester;
    }
  }
